---
phase: 06-paper-trading-validation
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - scripts/validate_pipeline.py
  - validation/BUG-LOG.md
  - validation/VALIDATION-REPORT.md
autonomous: false
requirements: [IBKR-05, EXEC-01, EXEC-02, EXEC-03, EXEC-04, EXEC-05, EXEC-06, RISK-01, RISK-02, RISK-03, RISK-04, IBKR-04, POS-01, POS-02, POS-03]
user_setup:
  - service: ib-gateway
    why: "D-09 mandates a real (non-mocked) IB Gateway paper account for the end-to-end run. Operator must start Gateway and complete 2FA — automated 2FA is explicitly out of scope."
    dashboard_config:
      - task: "Start IB Gateway in paper mode on bravos-vm1 (port 4002), log in, complete 2FA"
        location: "bravos-vm1"
      - task: "Provide 10+ real Bravos post URLs covering all 4 action types (open, add, partial_close, close) per D-01/D-02"
        location: "operator-supplied list, pasted into URL_LIST in scripts/validate_pipeline.py"
must_haves:
  truths:
    - "At least 10 real Bravos posts processed end-to-end via validate_pipeline.py (SC-1)"
    - "All 4 action types (open, add, partial_close, close) are represented in the run (D-02 / SC-2)"
    - "No order reached IBKR with wrong ticker, wrong action type, or miscalculated quantity (SC-2)"
    - "Every blocking bug found during the run is logged in BUG-LOG.md, fixed in-place, and re-verified (D-04/D-05)"
    - "VALIDATION-REPORT.md records PASS/FAIL per SC-1..SC-4 with evidence"
    - "Live observation period confirmed INGST-07, IBKR-02, IBKR-04 timing-sensitive behaviors (SC-4)"
  artifacts:
    - path: validation/VALIDATION-REPORT.md
      provides: "Completed pass/fail per success criterion"
      contains: "SC-1"
    - path: validation/BUG-LOG.md
      provides: "All bugs found, root cause, fix reference, status"
      contains: "Phase 6 Bug Log"
  key_links:
    - from: "validate_pipeline.py run"
      to: "validation/VALIDATION-REPORT.md"
      via: "per-scenario results transcribed + SC verdicts"
      pattern: "SC-[1-4]"
    - from: "blocking bug"
      to: "in-place code fix + re-run"
      via: "BUG-LOG.md entry with fix reference"
      pattern: "blocking"
---

<objective>
Execute the live paper-trading validation run on bravos-vm1 during NYSE market hours, log and fix every blocking bug in-place, and finalize the two validation artifacts. This is the phase's terminal plan — Phase 6 does not close until SC-1..SC-4 are satisfied (D-04).

Purpose: Per the phase goal, the full pipeline (scrape → parse → risk → order → fill → reconcile) must be exercised end-to-end on the paper account with real Bravos signals, with no critical failures left unresolved. D-09 forbids a mocked environment; D-10 forbids bypassing the market-hours gate (so the run must occur during 09:30–16:00 ET).
Output: Completed validation/VALIDATION-REPORT.md (SC-1..SC-4 verdicts), validation/BUG-LOG.md (all bugs + fixes), any in-place bug-fix commits.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-paper-trading-validation/06-CONTEXT.md
@.planning/phases/06-paper-trading-validation/06-RESEARCH.md
@.planning/phases/06-paper-trading-validation/06-PATTERNS.md
@.planning/phases/06-paper-trading-validation/06-02-SUMMARY.md
@scripts/validate_pipeline.py
@validation/BUG-LOG.md
@validation/VALIDATION-REPORT.md

<interfaces>
<!-- Built in Plan 02 — use as-is. -->
scripts/validate_pipeline.py — exposes main(), assert_signal_processed(), wait_for_fill(), run_startup(); URL_LIST is a guarded placeholder to be populated with operator URLs.
Run command (RESEARCH): `/home/chris_s_dodd/miniconda3/bin/python scripts/validate_pipeline.py`
Full test suite (regression after any fix): `/home/chris_s_dodd/miniconda3/bin/python -m pytest tests/ --ignore=tests/test_ingestion_integration.py --ignore=tests/test_infrastructure.py -v`
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator setup — IB Gateway + URL list + market-hours window</name>
  <files>(no files — operator-action checkpoint; inputs feed Task 2)</files>
  <action>
    CHECKPOINT (human-action — no CLI/API substitute exists). Pause execution and present the operator the prerequisites in <how-to-verify>. These three items are operator-only per D-09 (no mocked environment), D-01/D-02 (real curated URLs), and D-10 (market hours): start IB Gateway paper on bravos-vm1 and complete 2FA (automated 2FA is explicitly out of scope), supply the 10+ URL triples covering all 4 action types, and confirm the NYSE market-hours window. Do NOT proceed to Task 2, do NOT mock Gateway, do NOT invent URLs, do NOT bypass the market-hours gate. Resume only after the operator supplies the URL triples and confirms Gateway + market hours (or aborts).
  </action>
  <what-built>
    Plan 01 produced a green test suite + applied the risk_gate_log migration. Plan 02 produced scripts/validate_pipeline.py (with a guarded empty URL_LIST) and the two validation artifact scaffolds. Everything is ready to run except the three things only the operator can supply.
  </what-built>
  <how-to-verify>
    Before resuming, the operator must complete ALL of the following on bravos-vm1 (D-09 — no mocked environment, D-01/D-02 — real URLs, D-10 — market hours):

    1. Start IB Gateway in paper mode (port 4002) on bravos-vm1, log in, and complete 2FA. Confirm it is accepting connections (automated 2FA is explicitly out of scope — operator-only).
    2. Provide a list of 10+ real Bravos Research post URLs. The list MUST collectively cover all 4 action types: `open`, `add`, `partial_close`, `close` (D-02 — SC-2 requires every action type exercised). For each URL, note the expected ticker and expected action type.
    3. Confirm the current time is within NYSE regular trading hours (09:30–16:00 America/New_York). Per D-10 the market-hours gate is NOT bypassed — order→fill→reconcile only exercises in-hours. Out-of-hours runs will show `gate_passed=False, reason=market_hours` (expected, not a bug — RESEARCH Pitfall 3) and will NOT satisfy SC-1's order/fill requirement.

    Paste the URL list (url, expected_ticker, expected_action triples) for Task 2 to insert into URL_LIST.
  </how-to-verify>
  <verify>Operator has supplied >=10 URL triples covering all 4 action types AND confirmed IB Gateway paper running + NYSE market hours (manual confirmation — checkpoint gate).</verify>
  <resume-signal>Reply with the URL list (>=10, covering open/add/partial_close/close) AND confirmation that IB Gateway paper is running and it is currently NYSE market hours. Type "abort" to stop if Gateway/market-hours cannot be arranged now.</resume-signal>
  <done>Operator has provided the URL triples and confirmed Gateway + market-hours window; execution may proceed to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Run validation harness, log bugs, fix blocking bugs in-place, re-run until green</name>
  <files>scripts/validate_pipeline.py, validation/BUG-LOG.md, plus any source files requiring blocking-bug fixes</files>
  <read_first>
    - scripts/validate_pipeline.py (full — confirm URL_LIST placeholder location and structure)
    - validation/BUG-LOG.md (the scaffold table to append to)
    - .planning/phases/06-paper-trading-validation/06-CONTEXT.md (D-04 fix-in-place policy, D-05 blocking threshold)
    - .planning/phases/06-paper-trading-validation/06-RESEARCH.md (Pitfalls 1–6 — distinguish expected behavior from real bugs)
  </read_first>
  <action>
    1. Insert the operator-provided URL triples into `URL_LIST` in `scripts/validate_pipeline.py`, replacing the placeholder. Keep the format `(url, expected_ticker, expected_action)`. Verify all 4 action types appear (`open`, `add`, `partial_close`, `close`) — if any is missing, STOP and request the missing URLs from the operator (D-02 / SC-2 is non-negotiable).

    2. Run the harness on bravos-vm1:
       ```bash
       /home/chris_s_dodd/miniconda3/bin/python scripts/validate_pipeline.py 2>&1 | tee validation/run-$(date +%Y%m%d-%H%M%S).log
       ```

    3. For each FAIL line, classify per D-05:
       - **BLOCKING**: order not placed when it should have been, OR wrong ticker / wrong action type / miscalculated quantity. These MUST be fixed in-place in this plan (D-04 — phase does not close otherwise).
       - **NON-BLOCKING**: parser edge cases or log noise that do NOT corrupt the order path. Log them but they do not gate phase close.
       - **EXPECTED-NOT-A-BUG**: `reason=market_hours` block when out-of-hours, low-confidence signal-only, duplicate post (signal_id None) — per RESEARCH Pitfalls 2/3/6. Do NOT log these as bugs; note them in the report's per-scenario detail.

    4. For every real bug (blocking or non-blocking), append a row to `validation/BUG-LOG.md`: ID, date, severity, symptom, root cause, fix reference (file/commit), status. For BLOCKING bugs: fix the root cause in the relevant source file (scraper/parser/executor/gate/connection/positions as applicable), then re-run the full regression suite `pytest tests/ --ignore=tests/test_ingestion_integration.py --ignore=tests/test_infrastructure.py -v` to confirm no regression, then re-run validate_pipeline.py for the affected scenario(s).

    5. Repeat the run → classify → fix → re-run loop until: all scenarios PASS or are EXPECTED-NOT-A-BUG, every BLOCKING bug is fixed and re-verified, and the full regression suite is still green (0 failed). Do NOT close out with any unresolved blocking bug (D-04).

    Do NOT bypass the market-hours gate, do NOT mock IB Gateway, do NOT mark a scenario PASS without DB evidence (D-08 — use the script's signals/risk_gate_log/orders/executions/position_lots assertions).
  </action>
  <verify>
    <automated>/home/chris_s_dodd/miniconda3/bin/python -m pytest tests/ --ignore=tests/test_ingestion_integration.py --ignore=tests/test_infrastructure.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - URL_LIST in scripts/validate_pipeline.py contains >= 10 entries (`grep -cE '\("https?://' scripts/validate_pipeline.py` >= 10) and all 4 action types appear (`grep -oE '"(open|add|partial_close|close)"' scripts/validate_pipeline.py | sort -u | wc -l` == 4)
    - At least one validation run log exists (`ls validation/run-*.log` exits 0)
    - validation/BUG-LOG.md has every discovered real bug with a non-empty Root Cause and Fix Reference; `grep -c "blocking" validation/BUG-LOG.md` reflects classified entries (or the explicit "no blocking bugs found" line if none)
    - No BUG-LOG.md row has Status other than "Fixed" or "Non-blocking — deferred" (zero "Open"/"Blocking-unresolved" rows): `grep -iE "open|unresolved" validation/BUG-LOG.md` returns no blocking-status rows
    - Full regression suite exits 0 (0 failed) after all in-place fixes
  </acceptance_criteria>
  <done>validate_pipeline.py run against 10+ real URLs covering all 4 action types; all blocking bugs logged + fixed in-place + re-verified; regression suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize VALIDATION-REPORT.md (SC-1..SC-4 verdicts + per-scenario + live-observation)</name>
  <files>validation/VALIDATION-REPORT.md</files>
  <read_first>
    - validation/VALIDATION-REPORT.md (the scaffold to complete)
    - validation/BUG-LOG.md (cross-reference SC-3 against logged parser bugs)
    - the validation run log(s) under validation/run-*.log (source of per-scenario results)
    - .planning/phases/06-paper-trading-validation/06-RESEARCH.md (Success Criteria Mapping — SC-1..SC-4 definitions)
  </read_first>
  <action>
    Complete `validation/VALIDATION-REPORT.md` from the actual run evidence:

    1. **Per-Scenario Results table**: one row per URL — url, expected ticker, expected action, PASS/FAIL, detail (transcribe from validate_pipeline.py output / run log).

    2. **SC verdicts** (mark PASS only with concrete evidence; otherwise FAIL with explanation):
       - SC-1: PASS iff >= 10 real posts processed end-to-end (signal+order+fill rows where in-hours). Cite the count.
       - SC-2: PASS iff zero orders had wrong ticker/action/quantity AND all 4 action types were exercised (D-02). Cite the action-type coverage.
       - SC-3: PASS iff every parser edge case in BUG-LOG.md is marked Fixed and re-tested. Cross-reference BUG-LOG IDs.
       - SC-4: filled by Task 4 (live observation) — set to PENDING here, Task 4 updates it.

    3. Update **Status** line: keep "IN PROGRESS — awaiting live observation (SC-4)" until Task 4 completes; the report is not final until SC-4 is resolved.

    Do not mark any SC as PASS without citing the specific evidence (row counts, BUG-LOG IDs, action-type list). If an SC is FAIL, the phase cannot close — escalate per D-04 (this becomes a gap for `/gsd:plan-phase --gaps`).
  </action>
  <verify>
    <automated>grep -qE "SC-1.*\|.*(PASS|FAIL)" validation/VALIDATION-REPORT.md && grep -qE "SC-2.*\|.*(PASS|FAIL)" validation/VALIDATION-REPORT.md && grep -qE "SC-3.*\|.*(PASS|FAIL)" validation/VALIDATION-REPORT.md && ! grep -qE "SC-1.*PENDING" validation/VALIDATION-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - SC-1, SC-2, SC-3 each have a PASS or FAIL verdict (no PENDING) with cited evidence in the Evidence column
    - Per-Scenario Results table has >= 10 data rows (`grep -cE '^\| https?://' validation/VALIDATION-REPORT.md` >= 10)
    - SC-2 evidence explicitly lists which action types were exercised and confirms all 4 (open/add/partial_close/close)
    - SC-3 evidence references BUG-LOG.md entry IDs (or states "no parser bugs found")
    - SC-4 row still present and marked PENDING (Task 4 owns it)
  </acceptance_criteria>
  <done>VALIDATION-REPORT.md has evidence-backed PASS/FAIL for SC-1/SC-2/SC-3 and a populated per-scenario table; SC-4 left PENDING for Task 4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live observation period — confirm SC-4 timing-sensitive recoveries</name>
  <files>validation/VALIDATION-REPORT.md (SC-4 row + Status updated post-checkpoint), validation/BUG-LOG.md (if any critical failure observed)</files>
  <action>
    CHECKPOINT (human-verify — timing-sensitive, not scriptable). Pause execution and present the operator the daemon-observation steps in <how-to-verify>. Per D-03 the daemon is left running for at least one full NYSE trading day to observe INGST-07 (session expiry auto-recovery), IBKR-02 (heartbeat failure auto-recovery), and IBKR-04 (periodic reconciliation). After the operator reports findings: update VALIDATION-REPORT.md SC-4 to PASS/FAIL with the cited log evidence and set the Status line to its final value; if a critical unrecovered failure was observed, append it to BUG-LOG.md as BLOCKING and do NOT close the phase (escalate to /gsd:plan-phase --gaps per D-04).
  </action>
  <what-built>
    The seeded batch passed (SC-1..SC-3 verdicts recorded). Per D-03, the daemon (scripts/run_ingestion.py) is now left running to observe live incoming alerts and timing-sensitive recovery behaviors that cannot be scripted: INGST-07 (session expiry auto-recovery), IBKR-02 (heartbeat failure auto-recovery), IBKR-04 (periodic reconciliation runs cleanly each 5-min cycle). RESEARCH SC-4 notes these are timing-dependent and not unit-testable.
  </what-built>
  <how-to-verify>
    Operator runs the daemon for at least one full trading day and confirms from logs:

    1. Start the live daemon on bravos-vm1: `/home/chris_s_dodd/miniconda3/bin/python scripts/run_ingestion.py` (or the existing systemd unit if configured). Leave it running across a full NYSE session.
    2. **IBKR-04**: grep the daemon log — `run_periodic_reconciliation` runs every cycle with no unhandled exception; any internal-vs-IBKR position discrepancy is logged and flagged (not silently dropped).
    3. **IBKR-02**: confirm at least one heartbeat cycle completed; if a disconnect/CLOSE-WAIT occurred, confirm the log shows automatic force-reconnect with no operator intervention. (If no disconnect occurred naturally, note "no disconnect observed during window — heartbeat ran clean" — this is acceptable evidence.)
    4. **INGST-07**: confirm the scraper's post-cycle session check ran; if a session expiry occurred, confirm auto re-authentication in the log. (If session stayed valid, note "session remained valid through window — re-auth path not triggered" — acceptable.)
    5. Confirm NO critical failure occurred (scraper session expiry NOT auto-recovered, IBKR heartbeat failure NOT auto-recovered, or any risk-gate bypass). Any such occurrence is a BLOCKING failure per the phase goal — log it in BUG-LOG.md and the phase cannot close.

    Report observations so SC-4 can be marked PASS/FAIL in VALIDATION-REPORT.md and the Status line set to its final value.
  </how-to-verify>
  <verify>Operator has reported INGST-07 / IBKR-02 / IBKR-04 observations and an SC-4 PASS/FAIL verdict (manual — timing-sensitive checkpoint gate).</verify>
  <resume-signal>Reply with the live-observation findings for INGST-07 / IBKR-02 / IBKR-04 and an overall "SC-4 PASS" or "SC-4 FAIL: <reason>". After this, the executor updates VALIDATION-REPORT.md SC-4 + Status and the phase closes (or escalates to /gsd:plan-phase --gaps if any SC FAILed).</resume-signal>
  <done>SC-4 marked PASS/FAIL in VALIDATION-REPORT.md with cited log evidence; Status line finalized; any critical failure logged BLOCKING in BUG-LOG.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bravos site → daemon | live untrusted alert content over a full session |
| daemon → IB Gateway (paper) | real paper orders submitted across the trading day |
| operator → URL_LIST | operator-supplied URLs become the validation corpus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Elevation of privilege | risk-gate bypass during live run | mitigate | SC-4 explicitly checks for any risk bypass; a bypass is a BLOCKING failure logged in BUG-LOG and blocks phase close (phase goal + D-04) |
| T-06-09 | Spoofing | wrong ticker/action/quantity reaching paper IBKR | mitigate | SC-2 verdict requires zero incorrect orders across all 4 action types; per-scenario DB assertions compare to operator-declared expected values |
| T-06-10 | Denial of service | unrecovered scraper/heartbeat failure during full day | mitigate | SC-4 live observation specifically validates INGST-07 + IBKR-02 auto-recovery; unrecovered failure is blocking |
| T-06-11 | Repudiation | validation result without evidence | mitigate | VALIDATION-REPORT requires cited evidence (row counts, BUG-LOG IDs, log greps) per SC; run log archived under validation/run-*.log |
</threat_model>

<verification>
- validate_pipeline.py run against >=10 real URLs covering all 4 action types
- All blocking bugs logged in BUG-LOG.md, fixed in-place, regression suite green
- VALIDATION-REPORT.md: evidence-backed verdict for every SC-1..SC-4
- Phase closes only if SC-1..SC-4 all PASS; any FAIL escalates to /gsd:plan-phase --gaps (D-04)
</verification>

<success_criteria>
- SC-1: >=10 real Bravos posts processed end-to-end (evidence cited)
- SC-2: zero incorrect orders; all 4 action types exercised (D-02)
- SC-3: all parser edge cases fixed + re-tested (BUG-LOG cross-referenced)
- SC-4: live observation confirms INGST-07/IBKR-02/IBKR-04; no critical unrecovered failure
- BUG-LOG.md: every bug has root cause + fix reference + non-open status
</success_criteria>

<output>
After completion, create `.planning/phases/06-paper-trading-validation/06-03-SUMMARY.md`
</output>
