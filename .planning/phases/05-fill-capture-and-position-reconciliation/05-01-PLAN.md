---
phase: 05-fill-capture-and-position-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/migrate_phase5.sql
  - tests/test_positions.py
autonomous: true
requirements:
  - EXEC-05
  - EXEC-06
  - IBKR-04
  - POS-01
  - POS-02
  - POS-03
tags:
  - phase-05
  - fill-capture
  - position-lots
  - test-stubs

must_haves:
  truths:
    - "infra/migrate_phase5.sql exists and is idempotent (ALTER TABLE ... ADD COLUMN IF NOT EXISTS)"
    - "tests/test_positions.py exists with 10 Phase 5 test stubs, all currently skipped via @pytest.mark.skip"
    - "Every Phase 5 requirement (EXEC-05, EXEC-06, IBKR-04, POS-01, POS-02, POS-03) has at least one matching test stub by docstring reference"
    - "Test file imports compile (file is syntactically valid Python)"
    - "Skipped tests show up in pytest collection but do not execute"
  artifacts:
    - path: "infra/migrate_phase5.sql"
      provides: "Idempotent SQL migration confirming Phase 5 columns exist (orders.fill_price, orders.filled_at)"
      contains: "ALTER TABLE orders ADD COLUMN IF NOT EXISTS fill_price"
    - path: "tests/test_positions.py"
      provides: "Wave 0 test stubs for fill capture + position lot management + periodic reconciliation"
      contains: "@pytest.mark.skip"
      min_lines: 250
  key_links:
    - from: "tests/test_positions.py"
      to: "bravos.execution.positions"
      via: "import inside test functions"
      pattern: "from bravos.execution.positions import"
    - from: "tests/test_positions.py"
      to: "bravos.broker.connection"
      via: "import inside test functions for _handle_exec_details, _update_order_filled, _update_order_partial"
      pattern: "from bravos.broker import connection"
---

<objective>
Establish the Phase 5 test scaffolding and schema migration BEFORE any production code is written. This plan creates:

1. A Wave 0 test file (`tests/test_positions.py`) with 10 fully-written test bodies wrapped in `@pytest.mark.skip` decorators — one per Phase 5 requirement behavior. Plans 05-02 and 05-03 will un-skip these tests as they land the corresponding implementation. This mirrors the established Phase 1/2/3/4 pattern (see STATE.md Decisions: "Wave 0 test stubs written with full bodies inside @pytest.mark.skip — future plans remove decorator, not rewrite test").

2. An idempotent SQL migration (`infra/migrate_phase5.sql`) that guards against the Phase 5 columns (`orders.fill_price`, `orders.filled_at`) being missing from the live DB. The columns are present in `schema.sql` but may have been added after the initial deployment.

Purpose: Lock the requirement-to-test mapping before implementation, and ensure DB columns are guaranteed present on bravos-vm1 before fill writes attempt to populate them.

Output:
- `infra/migrate_phase5.sql` (new, ~15 lines)
- `tests/test_positions.py` (new, ~300 lines — 10 stub test functions)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fill-capture-and-position-reconciliation/05-CONTEXT.md
@.planning/phases/05-fill-capture-and-position-reconciliation/05-RESEARCH.md
@.planning/phases/05-fill-capture-and-position-reconciliation/05-PATTERNS.md

# Pattern source for Wave 0 stubs (exact analog)
@tests/test_execution.py

# Pattern source for idempotent migration
@infra/migrate_phase4.sql

# Schema reference (confirms which columns already exist)
@infra/schema.sql

# Test fixture reference (db_connection fixture used by integration tests)
@tests/conftest.py

<interfaces>
<!-- Function signatures the Wave 0 stubs reference. Plans 05-02 and 05-03 will create these. -->

# Will be created in Plan 05-02 (bravos/execution/positions.py):
def open_lot(ticker: str, shares: int, entry_price: float, db_conn) -> None
def partial_close_lot(ticker: str, shares_to_close: int, exit_price: float, db_conn) -> None

# Will be created in Plan 05-03 (bravos/broker/connection.py module-level helpers):
def _handle_exec_details(db_conn, execution, contract) -> None
def _update_order_filled(db_conn, ibkr_order_id: int, avg_fill_price: float) -> None
def _update_order_partial(db_conn, ibkr_order_id: int, avg_fill_price: float) -> None
# IBApp.run_periodic_reconciliation(db_conn, timeout: float = 30) -> None
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create infra/migrate_phase5.sql (idempotent Phase 5 schema migration)</name>
  <files>infra/migrate_phase5.sql</files>
  <read_first>
    - infra/migrate_phase4.sql (exact analog — same idempotent migration pattern: header comment, IF NOT EXISTS, GRANT, COMMENT ON)
    - infra/schema.sql (confirm orders.fill_price, orders.filled_at, executions.exec_id are defined; this migration is a safety net for live DBs that predate those columns)
  </read_first>
  <action>
    Create a new file `infra/migrate_phase5.sql` with the following EXACT contents (no other content, no v1/v2/simplified variants):

    ```sql
    -- Phase 5: Fill Capture and Position Reconciliation — schema migration
    -- Verifies Phase 5 columns exist on the live DB. Safe to run even if columns
    -- already exist (IF NOT EXISTS makes this idempotent).
    -- Run via: psql -h 127.0.0.1 -U bravos -d bravos_trading -f infra/migrate_phase5.sql

    -- orders table: fill capture columns (already in schema.sql; guard for early-deployed DBs)
    ALTER TABLE orders ADD COLUMN IF NOT EXISTS fill_price NUMERIC(10,2);
    ALTER TABLE orders ADD COLUMN IF NOT EXISTS filled_at TIMESTAMPTZ;

    -- executions table: exec_id UNIQUE is already defined in schema.sql.
    -- position_lots and broker_positions_snapshot tables are also already in schema.sql.
    -- No new tables or constraints are needed in Phase 5.

    COMMENT ON COLUMN orders.fill_price IS 'Avg fill price from IBKR orderStatus Filled callback (Phase 5)';
    COMMENT ON COLUMN orders.filled_at IS 'Timestamp when order reached FILLED state (Phase 5)';

    GRANT ALL ON orders TO bravos;
    ```

    Do NOT add new tables, new indexes, or new columns beyond what is shown above. Per D-04 and RESEARCH "Schema Column Availability" pitfall, this migration is a safety net — `schema.sql` is authoritative for new deployments.

    The file should be ASCII-only and end with a single trailing newline.
  </action>
  <verify>
    <automated>test -f infra/migrate_phase5.sql && grep -q "ALTER TABLE orders ADD COLUMN IF NOT EXISTS fill_price" infra/migrate_phase5.sql && grep -q "ALTER TABLE orders ADD COLUMN IF NOT EXISTS filled_at" infra/migrate_phase5.sql && grep -q "GRANT ALL ON orders TO bravos" infra/migrate_phase5.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `infra/migrate_phase5.sql` exists
    - `grep -c "IF NOT EXISTS" infra/migrate_phase5.sql` returns >= 2 (idempotent guards on both ALTER TABLE statements)
    - `grep -c "ADD COLUMN IF NOT EXISTS fill_price" infra/migrate_phase5.sql` returns 1
    - `grep -c "ADD COLUMN IF NOT EXISTS filled_at" infra/migrate_phase5.sql` returns 1
    - `grep -c "COMMENT ON COLUMN orders.fill_price" infra/migrate_phase5.sql` returns 1
    - `grep -c "COMMENT ON COLUMN orders.filled_at" infra/migrate_phase5.sql` returns 1
    - `grep -c "GRANT ALL ON orders TO bravos" infra/migrate_phase5.sql` returns 1
    - File contains no `CREATE TABLE` (no new tables in Phase 5)
    - File contains no `DROP` statements (append-only migration policy)
  </acceptance_criteria>
  <done>Migration file exists, contains exactly the 2 idempotent ALTER TABLE statements, 2 COMMENT ON COLUMN statements, 1 GRANT statement, and no other DDL.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_positions.py with 10 Wave 0 test stubs (all @pytest.mark.skip)</name>
  <files>tests/test_positions.py</files>
  <read_first>
    - tests/test_execution.py (exact analog Wave 0 stub pattern — module docstring, imports, MagicMock cursor pattern, db_connection fixture for integration tests, @pytest.mark.skip with implementing-plan reason)
    - tests/conftest.py (db_connection fixture signature — used by integration tests)
    - bravos/broker/connection.py (current state of IBApp; confirms where _handle_exec_details, _update_order_filled, _update_order_partial will be added)
    - bravos/execution/executor.py (current state of executor module; confirms positions.py is the right import target)
    - infra/schema.sql (position_lots and executions column names referenced in test assertions)
    - .planning/phases/05-fill-capture-and-position-reconciliation/05-CONTEXT.md (D-01 through D-10 — test docstrings cite these)
    - .planning/phases/05-fill-capture-and-position-reconciliation/05-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map: 10 tests with exact names)
  </read_first>
  <action>
    Create `tests/test_positions.py` as a new file. The file MUST contain exactly the 10 test functions listed below, in this order, each wrapped in `@pytest.mark.skip(reason="implementing: <plan>")` where `<plan>` is the un-skipping plan number.

    File structure (mirror tests/test_execution.py header pattern verbatim):

    ```python
    """
    tests/test_positions.py — Phase 5 Fill Capture + Position Lot unit and integration tests.

    All tests are Wave 0 stubs (skipped). Each test body is the full intended
    implementation. Tests are unskipped as their implementing plan lands:
      - 05-02: test_open_lot_writes_row,
               test_fifo_closes_oldest_lot_first,
               test_fifo_partial_close_one_lot,
               test_fifo_close_spanning_multiple_lots,
               test_close_lot_sets_fields
      - 05-03: test_exec_details_writes_execution_row,
               test_exec_details_idempotent,
               test_order_status_filled,
               test_order_status_partial,
               test_periodic_reconciliation_mismatch

    Per CONTEXT.md decisions D-01 through D-10.
    """
    import os
    from unittest.mock import MagicMock, patch
    import pytest
    ```

    Then write exactly these 10 test functions. Each MUST be preceded by `@pytest.mark.skip(reason="implementing: 05-XX-<short-tag>")`.

    ────────────────────────────────────────────────────────────────────────────
    Tests un-skipped by Plan 05-02 (positions.py module):
    ────────────────────────────────────────────────────────────────────────────

    1. `test_open_lot_writes_row(db_connection)` — POS-01 integration test.
       Skip reason: `"implementing: 05-02-open-lot"`.
       Body: imports `open_lot` from `bravos.execution.positions`, calls `open_lot("AAPL_05P1", 10, 150.00, db_connection)`, queries `SELECT quantity, entry_price FROM position_lots WHERE ticker='AAPL_05P1' AND lot_closed_at IS NULL ORDER BY lot_opened_at DESC LIMIT 1`, asserts `row[0] == 10` and `float(row[1]) == 150.00`. Cleanup: `db_connection.rollback()`.
       Use a ticker suffix like `_05P1` to avoid colliding with real positions.

    2. `test_fifo_closes_oldest_lot_first(db_connection)` — POS-03 integration test.
       Skip reason: `"implementing: 05-02-fifo"`.
       Body: insert two lots for ticker `"FIFO_T1"`: first lot (10 shares, $100 entry, lot_opened_at NOW() - interval '1 hour'), second lot (10 shares, $110 entry, lot_opened_at NOW()). Call `partial_close_lot("FIFO_T1", 10, 120.00, db_connection)`. Assert the OLDER lot has `lot_closed_at IS NOT NULL`, `exit_price == 120.00`, `pnl == (120.00 - 100.00) * 10 == 200.00`. Assert the NEWER lot is untouched: `lot_closed_at IS NULL`, `quantity == 10`. Cleanup: `db_connection.rollback()`.

    3. `test_fifo_partial_close_one_lot(db_connection)` — POS-03/AUDIT-04 integration test.
       Skip reason: `"implementing: 05-02-fifo"`.
       Body: insert one lot for `"PARTIAL_T1"` (20 shares, $50 entry). Call `partial_close_lot("PARTIAL_T1", 8, 60.00, db_connection)`. Assert two rows now exist for `"PARTIAL_T1"`: (a) the surviving OPEN lot has `quantity == 12` and `lot_closed_at IS NULL`; (b) an appended CLOSED row has `quantity == 8`, `lot_closed_at IS NOT NULL`, `exit_price == 60.00`, `pnl == (60.00 - 50.00) * 8 == 80.00`. This verifies AUDIT-04 append-only behavior. Cleanup: `db_connection.rollback()`.

    4. `test_fifo_close_spanning_multiple_lots(db_connection)` — POS-03 integration test.
       Skip reason: `"implementing: 05-02-fifo"`.
       Body: insert three lots for `"SPAN_T1"`: lot_a (10 shares, $90 entry, oldest), lot_b (20 shares, $95 entry, middle), lot_c (20 shares, $100 entry, newest). Call `partial_close_lot("SPAN_T1", 20, 110.00, db_connection)`. Assertions: lot_a fully closed (`lot_closed_at IS NOT NULL`, `quantity == 10`, `pnl == 200.00`); lot_b PARTIALLY closed — its surviving open row has `quantity == 10`, a new closed row exists with `quantity == 10`, `pnl == (110.00 - 95.00) * 10 == 150.00`; lot_c untouched (`lot_closed_at IS NULL`, `quantity == 20`). Cleanup: `db_connection.rollback()`.

    5. `test_close_lot_sets_fields(db_connection)` — POS-02 integration test.
       Skip reason: `"implementing: 05-02-close"`.
       Body: insert one lot for `"CLOSE_T1"` (5 shares, $200 entry). Call `partial_close_lot("CLOSE_T1", 5, 210.00, db_connection)` (full close = call with shares == total open qty per D-06). Assert the lot row now has `lot_closed_at IS NOT NULL`, `exit_price == 210.00`, `pnl == (210.00 - 200.00) * 5 == 50.00`. Cleanup: `db_connection.rollback()`.

    ────────────────────────────────────────────────────────────────────────────
    Tests un-skipped by Plan 05-03 (connection.py callbacks + reconciliation):
    ────────────────────────────────────────────────────────────────────────────

    6. `test_exec_details_writes_execution_row()` — EXEC-05 unit test (mock db).
       Skip reason: `"implementing: 05-03-exec-details"`.
       Body: import `from bravos.broker import connection`. Build a MagicMock `mock_conn` with `mock_conn.cursor.return_value.__enter__.return_value = mock_cur`. Set `mock_cur.fetchone.return_value = (42, "BUY")` (so the order lookup returns db_order_id=42, action=BUY). Build a mock `execution` with attributes `execId="EXEC_ABC"`, `orderId=1000`, `side="BOT"`, `shares=5`, `price=150.00`. Build a mock `contract` with `symbol="AAPL"`. Patch `bravos.execution.positions.open_lot` and call `connection._handle_exec_details(mock_conn, execution, contract)`. Assert: (a) `mock_cur.execute` was called at least twice (one SELECT, one INSERT executions); (b) one call matches the pattern `INSERT INTO executions` with `ON CONFLICT (exec_id) DO NOTHING` in the SQL; (c) `bravos.execution.positions.open_lot` was called once with args `("AAPL", 5, 150.00, mock_conn)`.

    7. `test_exec_details_idempotent()` — EXEC-05 unit test (mock db).
       Skip reason: `"implementing: 05-03-exec-details"`.
       Body: Same setup as test 6. Assert that the INSERT executions SQL string passed to `mock_cur.execute` contains the substring `"ON CONFLICT (exec_id) DO NOTHING"` (case-insensitive). This proves idempotency is wired regardless of DB outcome.

    8. `test_order_status_filled()` — EXEC-06 unit test (mock db).
       Skip reason: `"implementing: 05-03-order-status"`.
       Body: import `from bravos.broker import connection`. Build a MagicMock `mock_conn` and `mock_cur` as above. Call `connection._update_order_filled(mock_conn, ibkr_order_id=1000, avg_fill_price=150.25)`. Assert exactly one `mock_cur.execute` call was made whose SQL string contains both `UPDATE orders SET status='FILLED'` AND `fill_price=%s` AND `filled_at=NOW()` AND `WHERE ibkr_order_id=%s` (substring matches). Assert the parameters tuple is `(150.25, 1000)`.

    9. `test_order_status_partial()` — EXEC-06 unit test (mock db).
       Skip reason: `"implementing: 05-03-order-status"`.
       Body: same shape as test 8, but call `connection._update_order_partial(mock_conn, 1001, 149.75)`. Assert the SQL contains `UPDATE orders SET status='PARTIAL'` and `fill_price=%s` and `WHERE ibkr_order_id=%s`. Assert parameters tuple is `(149.75, 1001)`.

    10. `test_periodic_reconciliation_mismatch(db_connection)` — IBKR-04 integration test.
        Skip reason: `"implementing: 05-03-periodic-reconciliation"`.
        Body: import `from bravos.broker.connection import _reconcile_against_db`. Insert one open lot in `position_lots`: `("RECON_T1", 100 shares, $50, NOW(), NULL, NULL, NULL)`. Build `ibkr_positions = [{"ticker": "RECON_T1", "position": 50, "avg_cost": 50.0}]` (50 != 100 — mismatch). Patch the `bravos.broker.connection.logger` to capture warning calls. Call `_reconcile_against_db(db_connection, ibkr_positions, [])`. Assert at least one `logger.warning` call contains the substring `"RECONCILE MISMATCH"` AND `"RECON_T1"`. Cleanup: `db_connection.rollback()`.

    For ALL integration tests using `db_connection` fixture: insert any required test data with `ON CONFLICT DO NOTHING` or use unique tickers (e.g. with random suffixes via `os.urandom(4).hex()`) to avoid collisions. Always call `db_connection.rollback()` in a `try/finally` or at end of test body.

    For ALL tests: `@pytest.mark.skip` MUST be present at this stage (Wave 0). Plans 05-02 and 05-03 remove the decorator — they do NOT rewrite the body.

    Do NOT add any TODO comments, stub bodies, `pass`, `NotImplementedError`, or "to be implemented later" text. The body is FULL implementation today; only the skip decorator suppresses it. This matches the established pattern from Phases 1–4.
  </action>
  <verify>
    <automated>/home/chris_s_dodd/miniconda3/bin/python -m pytest tests/test_positions.py --collect-only -q 2>&1 | grep -c "test_" | awk '$1 >= 10 {exit 0} {exit 1}' && /home/chris_s_dodd/miniconda3/bin/python -m pytest tests/test_positions.py -x 2>&1 | grep -E "(skipped|10 skipped)"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_positions.py` exists
    - `/home/chris_s_dodd/miniconda3/bin/python -c "import ast; ast.parse(open('tests/test_positions.py').read())"` exits 0 (syntactically valid Python)
    - `grep -c "^def test_" tests/test_positions.py` returns exactly 10
    - `grep -c "@pytest.mark.skip" tests/test_positions.py` returns exactly 10
    - `grep -c "def test_open_lot_writes_row" tests/test_positions.py` returns 1
    - `grep -c "def test_fifo_closes_oldest_lot_first" tests/test_positions.py` returns 1
    - `grep -c "def test_fifo_partial_close_one_lot" tests/test_positions.py` returns 1
    - `grep -c "def test_fifo_close_spanning_multiple_lots" tests/test_positions.py` returns 1
    - `grep -c "def test_close_lot_sets_fields" tests/test_positions.py` returns 1
    - `grep -c "def test_exec_details_writes_execution_row" tests/test_positions.py` returns 1
    - `grep -c "def test_exec_details_idempotent" tests/test_positions.py` returns 1
    - `grep -c "def test_order_status_filled" tests/test_positions.py` returns 1
    - `grep -c "def test_order_status_partial" tests/test_positions.py` returns 1
    - `grep -c "def test_periodic_reconciliation_mismatch" tests/test_positions.py` returns 1
    - `grep -c "implementing: 05-02" tests/test_positions.py` returns 5 (open_lot + 3 FIFO + close)
    - `grep -c "implementing: 05-03" tests/test_positions.py` returns 5 (2 exec_details + 2 order_status + reconciliation)
    - `grep -c "NotImplementedError\|TODO\|to be implemented" tests/test_positions.py` returns 0 (full bodies, not stubs)
    - `/home/chris_s_dodd/miniconda3/bin/python -m pytest tests/test_positions.py -x 2>&1 | grep -E "10 skipped"` succeeds (all 10 tests collected and skipped, none errored on collection)
  </acceptance_criteria>
  <done>Test file exists with exactly 10 fully-bodied test functions; all are marked `@pytest.mark.skip` with the correct implementing-plan reason; pytest collects all 10 and reports them as skipped (no collection errors).</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Migration sanity:** `grep -c "ADD COLUMN IF NOT EXISTS" infra/migrate_phase5.sql` returns 2.
2. **Test count:** `/home/chris_s_dodd/miniconda3/bin/python -m pytest tests/test_positions.py --collect-only -q 2>&1 | tail -1` shows "10 tests collected".
3. **All tests currently skip:** `/home/chris_s_dodd/miniconda3/bin/python -m pytest tests/test_positions.py -x 2>&1 | tail -1` reports "10 skipped".
4. **No regressions:** `/home/chris_s_dodd/miniconda3/bin/python -m pytest tests/ -x --ignore=tests/test_ingestion_integration.py 2>&1 | tail -3` shows no new failures (existing skips preserved).
5. **Per-plan test mapping verifiable:**
   - `grep -E "def test_(open_lot|fifo_|close_lot_)" tests/test_positions.py` returns 5 lines (Plan 05-02 batch)
   - `grep -E "def test_(exec_details|order_status_|periodic_reconciliation_)" tests/test_positions.py` returns 5 lines (Plan 05-03 batch)
</verification>

<success_criteria>
- `infra/migrate_phase5.sql` exists and is idempotent (no new tables, no destructive operations, ADD COLUMN guarded by IF NOT EXISTS).
- `tests/test_positions.py` exists with all 10 named tests, each fully bodied and each marked `@pytest.mark.skip`.
- `pytest tests/test_positions.py` collects 10 tests and reports 10 skipped, zero errors.
- Existing test suite (`pytest tests/ -x --ignore=tests/test_ingestion_integration.py`) does not regress.
- The test-to-requirement mapping is unambiguous: every Phase 5 requirement ID (EXEC-05, EXEC-06, IBKR-04, POS-01, POS-02, POS-03) is referenced in at least one test docstring.
</success_criteria>

<output>
After completion, create `.planning/phases/05-fill-capture-and-position-reconciliation/05-01-SUMMARY.md` documenting:
- Migration file written and verified
- 10 test stubs created, plan-to-test mapping (which tests Plan 05-02 will un-skip, which Plan 05-03 will un-skip)
- Confirmation `pytest tests/test_positions.py` reports "10 skipped" with no collection errors
- Note any deviation from the test names/assertions specified (none expected)
</output>
