---
phase: 02-signal-ingestion
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/test_ingestion.py
autonomous: true
requirements:
  - INGST-06
  - AUDIT-03
  - AUDIT-06

must_haves:
  truths:
    - "Duplicate post_url INSERT is silently ignored (dedup)"
    - "raw_html is stored for every signal record"
    - "parse_method and scraped_at are populated on every signal"
    - "No UPDATE statement is ever used on signals table (immutability)"
  artifacts:
    - path: "tests/test_ingestion.py"
      provides: "DB-level ingestion tests un-skipped and passing"
      min_lines: 40
  key_links:
    - from: "tests/test_ingestion.py"
      to: "signals table"
      via: "psycopg2 INSERT/SELECT"
      pattern: "INSERT INTO signals"
---

<objective>
Un-skip and run the DB-level ingestion tests that verify deduplication (ON CONFLICT DO NOTHING), raw_html storage, and audit field population (parse_method, scraped_at). These tests exercise the signals table directly via psycopg2 against the live Cloud SQL database.

Purpose: Validates the schema migration from plan 02-01 is correct and the DB constraints work as expected before the scraper writes real data. These tests are the safety net that ensures INGST-03 (dedup), INGST-06 (raw storage), and AUDIT-01/AUDIT-06 (immutability) hold at the database level.

Output: 3 DB integration tests passing against live Cloud SQL.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-signal-ingestion/02-CONTEXT.md
@.planning/phases/02-signal-ingestion/02-01-SUMMARY.md

@infra/schema.sql
@infra/migrate_signals_v2.sql
@tests/test_ingestion.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-skip DB ingestion tests and verify they pass</name>
  <files>tests/test_ingestion.py</files>
  <read_first>
    - tests/test_ingestion.py
    - tests/conftest.py
    - infra/schema.sql
  </read_first>
  <action>
1. Remove all `@pytest.mark.skip(reason="DB write layer not yet implemented (plan 02-04)")` decorators from `tests/test_ingestion.py`.

2. The test bodies already contain complete INSERT/SELECT/DELETE statements against the signals table. They use the `db_connection` fixture from conftest.py which connects to Cloud SQL via the Auth Proxy on 127.0.0.1:5432.

3. The tests validate:
   - `test_dedup_on_conflict` — INSERT same post_url twice with ON CONFLICT DO NOTHING, assert count == 1
   - `test_raw_html_stored` — INSERT with raw_html, assert it is retrievable unchanged
   - `test_audit_fields_populated` — INSERT with parse_method='regex' and scraped_at=NOW(), assert both are non-null

4. Run `~/miniconda3/bin/pytest tests/test_ingestion.py -x -q` and verify all 3 tests pass.

5. Run `~/miniconda3/bin/pytest tests/ -x -q` to verify the full suite still passes (including Phase 1 tests and the skipped tests from other Phase 2 files).
  </action>
  <verify>
    <automated>cd /home/chris_s_dodd/bravos && ~/miniconda3/bin/pytest tests/test_ingestion.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ingestion.py` reports 3 passed, 0 failed, 0 skipped
    - `grep -c "pytest.mark.skip" tests/test_ingestion.py` returns 0
    - `pytest tests/ -x -q` full suite passes (no regressions)
  </acceptance_criteria>
  <done>All 3 DB ingestion tests pass: dedup constraint works, raw_html is stored, audit fields (parse_method, scraped_at) are populated</done>
</task>

</tasks>

<verification>
- `pytest tests/test_ingestion.py -v` shows 3 passed
- `pytest tests/ -x -q` full suite passes
- No test data left in the signals table after cleanup (each test cleans up its fixture rows)
</verification>

<success_criteria>
- 3 DB tests passing: dedup, raw_html storage, audit fields
- Full suite passes with no regressions
- Schema migration from plan 02-01 validated as correct
</success_criteria>

<output>
After completion, create `.planning/phases/02-signal-ingestion/02-04-SUMMARY.md`
</output>
